# Iter 14 — Next Lever Decision ⭐ (영구 자산)

Generated: 2026-05-27
Iter 14 verdict: **Case A 확정** — MAX overdrive D90 visible 37 (baseline 6 대비 +31)으로
모델 architectural bottleneck 없음 증명. Iter 15 = interpolation sweep with guards.

---

## 1. 결정: Iter 15 = T1+T3 Combined Interpolation Sweep with D33 Guard

### Iter 14 핵심 발견 (3가지)
```text
1. Case A 확정: MAX overdrive D90 vis=37, D90 mx=67.9 — 모델은 폭증 가능 (architecture 정상)
2. 진짜 lever rank: T3 phase (+15 BUT broken) > T1 truss (+5 clean) > T2/T4 (~0)
3. iter10_signal_stack도 broken (D10/20 vis=4 — early visible)
   ↔ 이건 phaseAware enabled가 가진 부작용
```

### Iter 15 strategy (Case A interpolation)
```text
T1 (truss timing) + T3 (phase transition, with guard) 결합:
  - T3 강도 줄이고 (cellDiv=220, expansionMul=1.25 등) D33 guard
  - T1을 mild하게 (gddPerTruss=100 등)
  - target: D90 vis 22-28 (현실 reference), D33 mx≤3 유지, D60 mx in band [18,40]

interpolation scale 0.0 → 1.0:
  scale=0.0: baseline (D90 vis=6)
  scale=0.5: D90 vis=20 추정 (target zone)
  scale=1.0: MAX (D90 vis=37, over)
```

---

## 2. Iter 15 후보 lever (우선순위)

### 2.1 ⭐ 1순위 — T1+T3 Mild Combined Sweep
```text
Sweep axes (4 × 3 × 2 = 24 runs):
  cellDivisionDurationGDD: 200, 220, 240, 260  (T3 강도 조절)
  expansionPhaseGrowthMultiplier: 1.0, 1.25, 1.5  (T3 강도)
  gddPerTruss: 100, 120  (T1 강도)

고정 (T3 guard):
  cellExpansionDurationGDD = 500 (baseline)
  phaseAwareEnabled = true
  divisionPhaseMassFraction = 0.10
  cellDivisionStepDemandFraction = 0.10
  transitionZoneGDD = 40
  abortionLagDays = 10, fruitSetRate = 0.80 (baseline)
```

### 2.2 2순위 — phaseAware guard 강화 (Iter 10 잔존 bug 해결)
Iter 10 stack 자체가 D10/20 vis=4 (early visible) 부작용. phaseAware가 visibility-side에서
의도와 다른 효과. 별도 audit 필요.

**의심**: cellDivisionStepDemandFraction=0.10 + transitionZoneGDD=40이 cohort 초기에
visibility threshold (3mm)를 그대로 통과시키는 부작용. Iter 15에서 visibilityGate를 결합 필요?

### 2.3 3순위 — Visibility threshold dynamic (보수적 fallback)
minVisibleDiameterMm 3mm가 모든 day에 동일 — 너무 sensitive. D10-30에는 더 큰 threshold
필요할 수 있음 (관측-side, biology 무관).

---

## 3. Iter 15 명시적 배제 (Iter 6~14 학습)

- ❌ Source/canopy/sink_balance (Iter 8 + Iter 14 T4 모두 reject)
- ❌ Fruit set survival 단독 (Iter 14 T2 효과 거의 0)
- ❌ cumulativeCapTransitionZoneGDD (Iter 11 mechanism inactive)
- ❌ Gompertz parameter sweep (Iter 6 reject)
- ❌ T3 단독 over-aggressive (Iter 14 D33 broken)
- ❌ T1 단독 mild (Iter 14 +5 정도라 v0.12 promote 부족)

---

## 4. Interpolation 가능 여부 ⭐ (사용자 검토 #5 필수 항목)

**예 — 가능**.

근거:
1. baseline D90 vis=6 / mx=55.9 (모두 정상 range)
2. MAX D90 vis=37 / mx=67.9 (vis 과다 + mx over-band)
3. 두 지점 사이에 D90 vis=20-28 / mx=60-65 zone이 분명히 존재
4. T1+T3 가벼운 결합으로 도달 가능 (Iter 15 1순위)

**Interpolation 방향**:
- baseline → target zone: T1 (gddPerTruss 100) + T3 mild (cellDiv 220, expansionMul 1.25)
- 너무 적으면 ↑, 너무 많으면 ↓

---

## 5. cultivar JSONC promotion 계획

Iter 15 Strong Promote 통과 시:
```text
tomimaru-muchoo.jsonc 갱신:
  phenology.cellDivisionDurationGDD: 300 → <Iter 15 winner, 추정 220-240>
  phenology.GDD_per_truss: 120 → <Iter 15 winner, 추정 100-120>
  botanicalOverride.fruitDevelopment.gompertz: Iter 10 stack 유지 (0.55/0.05/0.08)
  botanicalOverride.fruitDevelopment.massFlow.phaseAwareMassGrowth:
    enabled: true
    divisionPhaseMassFraction: 0.10
    cellDivisionStepDemandFraction: 0.10
    transitionZoneGDD: 40
    expansionPhaseGrowthMultiplier: <Iter 15 winner, 추정 1.25>

→ v0.11.3 → v0.12-overdrive-interpolation-tuned 정식 진입
→ Single Plant mode → Skin이 실제로 v0.12 visible recovery 반영
```

---

## 6. Iter 14 학습의 SSOT 영구

```text
SSOT #152 (Iter 14 Case A confirmation):
  MAX overdrive D90 vis=37 (+31) — 모델은 토마토 폭증 가능. architecture bottleneck 없음.

SSOT #153 (Iter 14 lever rank finalized):
  T3 phase > T1 truss >> T2 fruit_set ≈ T4 source ≈ 0.
  Iter 15 이후 T2/T4 lever 재시도 금지.

SSOT #154 (Iter 14 interpolation sweep direction):
  T1 + T3 결합 with D33 guard. baseline ↔ MAX 사이 24-run sweep.

SSOT #155 (Iter 14 iter10_signal_stack 부작용):
  phaseAware enabled가 D10/20 fruit visibility threshold 부작용 가짐.
  Iter 15 audit 필수.
```

---

## 7. 한 문장 정리

```text
Iter 14 7-candidate overdrive theory test — MAX overdrive D90 vis=37 (baseline 6 대비 +31)
으로 Case A 확정 (모델 architecture 정상, interpolation 가능). 진짜 lever rank: T3 phase
transition (+15 BUT D33 broken) > T1 truss timing (+5 clean) >> T2 fruit set (≈0) ≈ T4 source
(≈0). Iter 15 1순위: T1 + T3 결합 24-run sweep with D33 guard. interpolation 가능 ✅
(baseline-target-MAX 사이 D90 vis 20-28 zone 분명). Strong Promote 시 cellDiv=220-240 +
gddPerTruss=100-120 + Iter 10 stack winner 값으로 v0.11.3 → v0.12-overdrive-interpolation-tuned
정식 진입.
```

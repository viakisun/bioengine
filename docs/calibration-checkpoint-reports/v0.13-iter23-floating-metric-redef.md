# Iter 23 — Floating Metric Semantic 재정의 + Peduncle radialDir 확장

Generated: 2026-05-28
Predecessor: Iter 22 (commits d9446e3, 435e2d7, a6a2be8 — approve)
Iter 23 commit: 1310aa8
Status: **Iter 23 부분 달성 — metric 재정의 ✓ + peduncle 부분 개선 ✓ /
acceptance 6/6 미달 (5/6, peduncle 1개 잔존). Iter 24 (peduncle 정식 fix)
후 자연 6/6 도달 예상.**

---

## 🎯 한 줄 결과

```text
floatingCount D45: 41 → 1 (-98%). metric semantic 정확화 + peduncle
radialDir Iter 19 fix 확장. 잔존 1개는 D45 peduncle t2 (6.54mm) — peduncle
truss 구조 특성상 radialDir만으론 부족, Iter 24 본 작업 필요.
Acceptance 5/6 (Iter 22와 동일하나 의미가 완전히 다름 — 이전엔 41 false
positive, 이제는 1 real disconnect).
```

---

## 1. 변경 (commit 1310aa8)

### 1.1 Metric semantic 재정의

[src/plant/skin/StemFamilyTubeNetworkBuilder.ts](src/plant/skin/StemFamilyTubeNetworkBuilder.ts):

```ts
// 기존 (Iter 18A)
const FLOATING_GAP_THRESHOLD_M = 0.001; // 1mm
const radialGap = vlen(vsub(childGraphStart, parentSurfacePoint));

// 신규 (Iter 23)
const FLOATING_GAP_THRESHOLD_M = 0.003; // 3mm
const renderedRootGap = vlen(vsub(childGraphStart, childStart));
```

**의미 변화**:
- 기존: PlantBase bio surface 점 ↔ runtime swollen surface 점. 둘은
  parentSwellingScale(1.25) × bio_radius = 0.25 × bio_radius 만큼 항상 차이.
  D99 bio 7.7mm 기준 ~1.9mm offset이 매 edge마다 가산되어 false positive
  다수.
- 신규: PlantBase 정한 attach point ↔ 실제 rendered mesh root. 시각적
  disconnect 직접 indicator (= Q1 expectedToActual).

**임계값 1mm → 3mm**: Iter 18C에서 정의한 severity classifier (`green ≤ 1mm,
yellow ≤ 3mm, red > 3mm`)와 일관. 1mm는 너무 strict (cosmetic only),
3mm는 사람 눈으로 disconnect로 보이는 경계.

### 1.2 Peduncle radialDir 확장

```ts
// 기존 (Iter 19)
const sourceDir = edge.type === 'petiole'
  ? vsub(childGraphStart, parentCenter)
  : averageTangent(edge.bonePath, ...);

// 신규 (Iter 23)
const sourceDir = (edge.type === 'petiole' || edge.type === 'peduncle')
  ? vsub(childGraphStart, parentCenter)
  : averageTangent(edge.bonePath, ...);
```

**근거**: Iter 19 Phase B 측정에서 peduncle도 동일 Case B 패턴 (D45 worst
9.78mm, D99 worst 12.59mm — averageTangent 기반 radialDir flip).

**효과**:
- D45 peduncle worst: 9.78 → 6.54mm (-33%)
- D99 peduncle worst: 12.59 → 8.48mm (-33%)
- 부분 개선 — peduncle 구조 (truss 시작점, vertical extension)는 petiole와
  근본적으로 다른 emerge 패턴이라 radialDir 단독으론 부족.

### 1.3 Peduncle embed clamp 시도 후 revert

petiole embed clamp [0.2,1.0]mm을 peduncle에도 적용 → floatingCount 1 → 2
역효과 (Q1 일부 case가 미세하게 악화). revert.

---

## 2. 측정 비교

### 2.1 floatingCount (D45 mode, AC1 측정점)

| Stage | Count | Composition |
|---|---:|---|
| Iter 22 (Iter 18A metric) | 41 | petiole 6, peduncle 3, rachis 16, pedicel 16 |
| Iter 23 (신규 metric, peduncle fix 전) | 3 | peduncle 3 (Case B real disconnect) |
| Iter 23 (peduncle radialDir 적용) | **1** | peduncle t2 (6.54mm) |

**98% 감소**. 잔존 1개는 false positive가 아닌 **real disconnect** (peduncle
구조적 잔존 문제).

### 2.2 petiole Q1 (Iter 21/22 invariant 확인)

| Day | Q1 worst (Iter 22) | Q1 worst (Iter 23) | 회귀 |
|---|---:|---:|---|
| D45 | 0.69mm | 0.69mm | 없음 |
| D99 | 2.23mm | 2.23mm | 없음 |

### 2.3 peduncle 후속 (Iter 19 Phase B 비교)

| Day | Iter 19 worst | Iter 23 worst | Δ |
|---|---:|---:|---:|
| D45 | 9.78mm | 6.54mm | -3.24 (-33%) |
| D99 | 12.59mm | 8.48mm | -4.11 (-33%) |

### 2.4 회귀 검증
- ✓ ShowcasePlant 9/9 PASS
- ✓ AC2-6 모두 PASS
- ✓ Tip→Leaf = 0mm invariant
- ✓ iter18c-docking-debug Q1 invariant (D45 0.69, D99 2.23)

---

## 3. Acceptance Criteria 결과

| AC | 기준 | 결과 | 판정 |
|---|---|---|---|
| AC23-01 | typecheck PASS | ✓ | PASS |
| AC23-02 | floating metric semantic 재정의 | ✓ | PASS |
| AC23-03 | 임계값 1mm → 3mm (severity 일관) | ✓ | PASS |
| AC23-04 | peduncle Iter 19 fix 확장 (D45/D99 -33%) | ✓ | PASS |
| AC23-05 | acceptance AC1 회복 (6/6) | ✗ partial (5/6, 1 real disconnect 잔존) | soft fail |
| AC23-06 | 회귀 없음 (AC2-6 + ShowcasePlant) | ✓ | PASS |

**5 PASS + 1 soft fail (AC23-05)**. 잔존 acceptance fail은 metric의 의미가
바뀐 결과 — 이전엔 41 false positive였고 이제 1 real disconnect. 진정한
6/6 회복은 Iter 24 peduncle 본 fix 후 자연 도달.

---

## 4. Iter 24 후속

사용자 기존 트랙 그대로:
- **Iter 24 — Peduncle Case B fix** (본 작업)
  - peduncle은 truss 구조 — radialDir 단독 + embed clamp만으로는 부족
  - 옵션 후보:
    1. peduncle bone path 첫 segment 재계산 (petiole 5cp c0 패턴 확장)
    2. peduncle용 별도 embed/swelling policy
    3. parentSwellingScale 1.25 → 1.15-1.18 (n16 D99 Q1 outlier도 같이 해소)
  - Iter 24 진입 시 다시 진단 + 옵션 선택
- **Iter 25**: Semantic Skeleton Engine read-only foundation

---

## 5. 사용자 wake 가이드

### 검토 자료
- 코드 변경 [src/plant/skin/StemFamilyTubeNetworkBuilder.ts](src/plant/skin/StemFamilyTubeNetworkBuilder.ts) 약 10 LOC
- acceptance 결과: AC1 41 → 1 (대폭 개선, semantic clean)
- 잔존 1개 = D45 peduncle t2 6.54mm = real disconnect (Iter 24 본 작업 대상)

### 판단 옵션

A. **Iter 23 부분 달성 인정 + Iter 24 진행** (추천)
   - metric 재정의는 깔끔히 끝남 (의미 정확화)
   - 6/6은 Iter 24에서 자연 도달 (peduncle 본 fix 후)
   - 사용자 트랙 순서대로

B. **Iter 23 안에서 peduncle 본 fix 시도** (Iter 24 흡수)
   - 추가 LOC (~30-50), 진단 + 옵션 선택 후 재측정 필요
   - 한 commit으로 깔끔하나 scope 확장

C. **threshold 3mm → 7mm 인위 조정** (반대)
   - AC1=0 통과하나 semantic 부정 — 6.54mm는 real disconnect, 가려선 안 됨

---

## 6. 한 문장 정리

```text
Iter 23 — Floating Metric Semantic 재정의 (childGraphStart vs parentSurfacePoint
→ childGraphStart vs childStart, 1mm → 3mm threshold) + peduncle radialDir
PlantBase azimuth 확장 (Iter 19 petiole fix를 peduncle에 적용). 결과: D45
floatingCount 41 → 1 (-98%), peduncle worst D45 9.78→6.54mm / D99 12.59→8.48mm.
잔존 1개는 D45 peduncle t2 real disconnect (peduncle truss 구조라 radialDir
단독 + embed clamp만으론 부족, Iter 24 본 작업 대상). petiole Q1 invariant,
ShowcasePlant 9/9 + AC2-6 회귀 없음. AC1만 ✗ partial — 의미는 완전히 다름
(이전 41 false positive, 이제 1 real). 사용자 트랙 순서대로 Iter 24 (peduncle
정식 fix) 후 acceptance 6/6 자연 도달 예상.
```

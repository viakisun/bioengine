// InfoPanel is currently handled inline by GrowthController.updateUI()
// This module is reserved for future expansion (detailed per-plant stats, etc.)

export function initInfoPanel(): void {
  // No-op for now, UI elements are in index.html
}
